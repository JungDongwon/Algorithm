// 1<=a,b<=10^9 이라는 조건때문에 O(n) 알고리즘도 시간 초과를 받게 된다.
// O(1)만에 답을 찾아야 한다.

#include <iostream>
using namespace std;
int main() {
	int t;
	cin >> t;
	while(t--){
		int a,b;
		cin >> a >> b;
		if(a%b==0) cout << 0 << '\n';
		else cout << b-a%b << '\n';
	}
}
